extends layout

block content
    h1 Confirm Admin Role
    form(method='POST')
        label(for='password') Password
        input(name='password' type='password')
        button Sign Up

    if errors
        ul
          for error in errors
            li!= error.msg